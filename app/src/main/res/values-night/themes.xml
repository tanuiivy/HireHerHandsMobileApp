<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.HHHApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color (your green) -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/primary_dark</item>
        <item name="colorOnPrimary">@color/button_text</item>

        <!-- Secondary brand color (lighter green) -->
        <item name="colorSecondary">@color/accent</item>
        <item name="colorSecondaryVariant">@color/success</item>
        <item name="colorOnSecondary">@color/text_primary</item>

        <!-- Error color -->
        <item name="colorError">@color/error</item>

        <!-- Background colors -->
        <item name="android:colorBackground">@color/background</item>
        <item name="colorSurface">@color/surface</item>

        <!-- Status bar color -->
        <item name="android:statusBarColor">@color/primary_dark</item>
    </style>
</resources>